cars
install.packages("stringr")
install.packages("stringr")
install.packages("RColorBrewer")
x
x = 2
3*x^2 + 4x + 8
3*x^2 + 4*x + 8
help("exp")
help.search("+")
help.search("binary operator")
# For selecting the files in the working directory (The current folder where the file locates)
help.search("operator")
getwd()
# read data
download.file(
url = 'https://raw.githubusercontent.com/ucb-stat133/stat133-fall-2016/master/data/womens-high-jump-records.csv',
destfile = '~/Desktop/STAT 133/stat133-my-repo/lab/womens-high-jump-records.csv')
dat <- read.csv("womens-high-jump-records.csv", stringsAsFactors = FALSE)
# take a peek
head(dat)
# your vector athlete_name
athlete_name = paste(dat$first, dat$last)
athlete_name
# your vector athlete
athlete_name_country = paste0(athlete_name,'(',dat$country,')')
athlete_name_country
# your vector athlete
athlete_name_country = paste0(athlete_name,' ','(',dat$country,')')
athlete_name_country
a <- "Jan"
switch(a,
"Jan" = month.name[1],
"Feb" = month.name[2],
"Mar" = month.name[3],
"Apr" = month.name[4],
"May" = month.name[5],
"Jun" = month.name[6],
"Jul" = month.name[7],
"Aug" = month.name[8],
"Sep" = month.name[9],
"Oct" = month.name[10],
"Nov" = month.name[11],
"Dec" = month.name[12],
)
# your expand_month() function
# This function is for expanding thenames of the months in abbreviated
# format to full name
expand_month = function(abr){
switch(abr,
"Jan" = month.name[1],
"Feb" = month.name[2],
"Mar" = month.name[3],
"Apr" = month.name[4],
"May" = month.name[5],
"Jun" = month.name[6],
"Jul" = month.name[7],
"Aug" = month.name[8],
"Sep" = month.name[9],
"Oct" = month.name[10],
"Nov" = month.name[11],
"Dec" = month.name[12]
)
}
# test it with expand_month("Apr") and expand_month("Xyz")
expand_month("Apr")
# your expand_month() function
# This function is for expanding thenames of the months in abbreviated
# format to full name
expand_month = function(abr){
switch(abr,
"Jan" = month.name[1],
"Feb" = month.name[2],
"Mar" = month.name[3],
"Apr" = month.name[4],
"May" = month.name[5],
"Jun" = month.name[6],
"Jul" = month.name[7],
"Aug" = month.name[8],
"Sep" = month.name[9],
"Oct" = month.name[10],
"Nov" = month.name[11],
"Dec" = month.name[12]
)
}
expand_month("Apr")
expand_month("Xyz")
# test it with expand_month("Apr") and expand_month("Xyz")
# your expand_month() function
# This function is for expanding thenames of the months in abbreviated
# format to full name
expand_month = function(abr){
switch(abr,
"Jan" = month.name[1],
"Feb" = month.name[2],
"Mar" = month.name[3],
"Apr" = month.name[4],
"May" = month.name[5],
"Jun" = month.name[6],
"Jul" = month.name[7],
"Aug" = month.name[8],
"Sep" = month.name[9],
"Oct" = month.name[10],
"Nov" = month.name[11],
"Dec" = month.name[12],
"NA"
)
}
expand_month("Apr")
expand_month("Xyz")
# test it with expand_month("Apr") and expand_month("Xyz")
new_month = c()
# your for loop to get new_month
for (abr in month){
full_name = expand_month(abr)
new_month = c(new_month, full_name)
}
new_month = c()
month = dat$month
# your for loop to get new_month
for (abr in month){
full_name = expand_month(abr)
new_month = c(new_month, full_name)
}
new_month
?lapply
?sapply
lapply(month, expand_month
lapply(month, expand_month)
lapply(month, expand_month())
lapply(month, function(x) expand_month(x))
sapply(month, function(x) expand_month(x))
full_month = sapply(month, function(x) expand_month(x))
?paste
record_dates = paste(full_month, day, year, sep = "-")
record_dates = paste(full_month, dat$day, dat$year, sep = "-")
record_dates
?as.Date
record_dates <- as.Date(record_dates, format = "%B-%m-%d")
record_dates <- as.Date(record_dates, format = "%B-%m-%d")
record_dates
record_dates <- as.Date(record_dates, format = "%B-%d-%Y")
record_dates
record_dates <- as.Date(record_dates,"%B-%d-%Y")
record_dates <- as.Date(record_dates, "%B-%d-%Y")
record_dates
dates = paste(day, full_month, year, sep = ' ')
dates = paste(dat$day, full_month, dat$year, sep = ' ')
dates
dates = paste(dat$day, full_month, dat$year)
record_dates <- as.Date(record_dates, "%B-%d-%Y")
record_dates
record_dates = paste(full_month, dat$day, dat$year, sep = "-")
record_dates2 <- as.Date(record_dates, format = "%B-%d-%Y")
record_dates
record_dates2
record_dates2 <- as.Date(record_dates, format = "%m-%d-%Y")
record_dates2
record_dates2 <- as.Date(record_dates, format = "%B-%d-%Y")
record_dates2
class(record_dates2)
record_dates2 <- as.Date(record_dates, format = "%B-%d-%Y")
record_dates2
dates = paste(dat$day, full_month, dat$year)
dates
dates = as.Date(dates, format = "%d %B %Y")
dates
dates2 = as.Date(dates, format = "%d %B %Y")
dates = paste(dat$day, full_month, dat$year)
dates2 = as.Date(dates, format = "%d %B %Y")
dates2
record_dates2 <- as.Date(record_dates, format = "%Y-%d-%B")
record_dates2
nrow(dat)
length(dat)
length(month)
